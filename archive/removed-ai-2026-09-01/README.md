# Removed AI feature archive

This directory preserves the standalone AI implementation removed from Bemo on 2026-09-01.

The files here are not imported by the application and are outside the frontend TypeScript build. They are retained only because repository policy requires confirmation before deletion.
