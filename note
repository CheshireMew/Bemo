# Bemo 开发与发布入口

当前架构以 CURRENT_ARCHITECTURE.md 为准。Web / Desktop 的主存储在 backend，Mobile 的主存储在本地；它们共享产品语义，但不共用所有存储实现或界面。

日常 Web 开发使用根目录 start-dev.ps1。桌面源码调试在 frontend 运行 npm run tauri -- dev，并通过 BEMO_PYTHON 指定已经安装依赖的 Python，通过 BEMO_DATA_DIR 指定现有数据目录。启动时不会安装依赖。

只有明确需要生成 Windows 发布产物时才运行 npm run build:desktop。这个入口会从当前 Python 源码生成后端，校验源文件和二进制摘要，再交给 Tauri；不要手动复制仓库里遗留的 exe，也不要用 main.py 生成后端程序。

依赖准备、已有数据位置、网络部署与验证说明见 docs/operations.md。移动端入口见 frontend/README.md；本轮只验收 Windows，不代表 Android 或其它平台发布通过。
